{% extends 'base.html.twig' %}

{% block title %}Hello TweitterController!
{% endblock %}

{% block body %}

	<section class="container my-4">
		<div class="row">
			<div class="col-12">
				<h2>
					Détail du tweet id	{{ tweet.id }}
        </h2>
			</div>
		</div>
	</section>

<div class="container px-4">
  <div class="row gx-5">
    <div class="col">
     <div class="p-3  bg-dark">{{tweet.favorite_count}} like(s)</div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark">{{tweet.retweet_count}} retwweet</div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark">? commentaire</div>
    </div>
  </div>
</div>

<div class="container px-4 mt-2">
  <div class="row gx-5">
    <div class="col">
     <div class="p-3  bg-dark"> <span>date creation</span><br>{{ tweet.created_at }}</div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark "><span>post</span><br>{{ tweet.full_text |nl2br  }}</div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark">? commentaire</div>
    </div>
  </div>
</div>

	
  

  
<div class="container px-4 mt-2">
  <div class="row gx-5">
    <div class="col">
     <div class="p-3  bg-dark">
      Listes des hashtags<br>
<ul>
      {% for hashtag in tweet.entities.hashtags %}
    <li>    {{ hashtag.text}}</li>
      {% endfor %}
</ul>
     </div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark">
      media<br>
 {% if tweet.entities.media  is defined %}


       {% for media in tweet.entities.media %}
       type : {{media.type}}
      <img class="img-fluid" src=" {{ media.media_url_https}}">
      {% endfor %}
<br>
<a href="#">Voir plus</a>
{% endif %} 
      </div>
    </div>
    <div class="col">
      <div class="p-3  bg-dark">? commentaire</div>
    </div>
  </div>
</div>
<pre>
  {{ dump(tweet) }}
</pre>
{% endblock %}
